if __name__ == '__main__':
    import random
    import uuid
    from faker import Faker

    faker = Faker()
    status = random.choice(['SUCESSO', 'FALHA'])
    detalhes = "Pagamento aprovado via PIX." if status == 'SUCESSO' else "Limite do cartao excedido."

    notificacao_simulada = {
        "pedido_id": str(uuid.uuid4()),
        "status": status,
        "detalhes": detalhes,
        "id_produto": faker.random_int(min=1, max=1000),
        "quantidade": faker.random_int(min=1, max=100),
        "email_cliente": faker.email()
    }

    print("--- Registrando log de evento de pedido ---")
    registrar_log(notificacao_simulada)
